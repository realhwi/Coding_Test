#define _CRT_SECURE_NO_WARNINGS
#include <cmath>
#include <cstdlib>
#include <ctime>
#include <cstring>
#include <vector>
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);  

    // 함수에서 코드는 항상 한번만 수행됨 
    printf("1\n");  // 코드 1의 수행 횟수 
    printf("0\n");  // 시간 복잡도는 O(1) = 최고차항 0 

    return 0;
}
